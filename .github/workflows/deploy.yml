name: Deploy FastAPI App

on:
    push:
        branches: [master]

jobs:
    deploy:
        runs-on: self-hosted
        steps:
            - name: Checkout latest code
              uses: actions/checkout@v3

            - name: Run FastAPI app
              run: |
                  ./run.sh
